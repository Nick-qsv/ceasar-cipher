*Quickly encrypt your passphrases using this highly secure cipher*

Multiple states, main menu, encrypt, decrypt, 

26 letter array of the alphabet
Vector for the passphrase

Passphrase is extended to length 26 by repeating it until it reaches len 26

first enter passphrase
then enter word (only string) you want to encrypt.  Need some error handling

Then find the first index where that letter occurs in the alphabet, store that index number x

Make a new string called M and push the index of PH[x]

loop and return x

//DECRYPTION

get an encrypted message input
get the password 